export class RoomConnectionModel {
    constructor(firstRoom, secondRoom, corridor) {
        this.firstRoom = firstRoom;
        this.secondRoom = secondRoom;
        this.corridor = corridor;
    }
}